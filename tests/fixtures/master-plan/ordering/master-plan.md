# fixture-ordering — Master Plan

## Preface

This fixture carries block records across three phases with interleaved
wave numbers, to pin the generator's ordering as deterministic: phases in
authored `tracks[]` order, blocks within a phase in authored (ascending
wave) order.

<!-- BEGIN generated:master-plan-body -->
(stale placeholder content the renderer must replace)
<!-- END generated:master-plan-body -->

## Appendix

Authored prose below the generated region.
