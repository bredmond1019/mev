# fixture-no-block-records — Master Plan

This repo has no blocks in `state.json`'s `tracks[]` (and no `planning/blocks/`
directory at all). The generator must leave this file completely untouched —
no read attempt should even matter, since the render is empty before the file
is ever opened.

<!-- BEGIN generated:master-plan-body -->
(hand-authored placeholder — must never be touched since there are no blocks
to render)
<!-- END generated:master-plan-body -->
